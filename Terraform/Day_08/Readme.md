# Here we learned about meta arguments in Terraform. Such as count, for_each, depends on

# both each.key and each.value are same in set but the difference will come when we will use map because there we use key value pair

# depends on means it will depend on other resource

