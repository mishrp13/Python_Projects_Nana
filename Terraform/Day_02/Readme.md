# In this section we understood about the Terraform providers and with the help of Terraform
# provider how can call AWS API. so HCL(Hashicorp Configuration language) --> Terraform providers -->
# AWS API . so this is the process and flow we write our code in json format (HCL) and then Terraform 
# providers does the API call. so we have make sure we are using compatible version of Terraform provider.